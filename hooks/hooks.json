{
  "description": "Security gate: scan code before shipping commands (git commit, push, PR)",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then python3 ${CLAUDE_PLUGIN_ROOT}/hooks/pre_commit_scan.py; else python ${CLAUDE_PLUGIN_ROOT}/hooks/pre_commit_scan.py; fi",
            "timeout": 10
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then python3 ${CLAUDE_PLUGIN_ROOT}/hooks/protect_scan_pass.py; else python ${CLAUDE_PLUGIN_ROOT}/hooks/protect_scan_pass.py; fi",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then python3 ${CLAUDE_PLUGIN_ROOT}/hooks/protect_scan_pass.py; else python ${CLAUDE_PLUGIN_ROOT}/hooks/protect_scan_pass.py; fi",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
